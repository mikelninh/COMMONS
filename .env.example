# Get a key from TypeSafe and never commit the real value.
TYPESAFE_API_KEY=

# Optional: pin after thresholds are calibrated against a version.
# TYPESAFE_DEFAULT_MODEL=jev-latest

# Local/pilot audit store. Public stateless /analyse does not require persistence.
COMMONS_DB_PATH=commons.db
